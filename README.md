
  # ZestOra Productivity App

  This is a code bundle for ZestOra Productivity App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  "# ZestOra" 
